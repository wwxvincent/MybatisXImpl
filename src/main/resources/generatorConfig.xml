<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!-- 数据库连接信息 -->
    <context id="mysql" targetRuntime="MyBatis3">
        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://localhost:3306/mybatis_x_test"
                        userId="root" password="vincent2017727">
        </jdbcConnection>

        <!-- Java模型生成器 -->
        <javaModelGenerator targetPackage="com.vincent.mybatixtest"
                            targetProject="src/main/java">
            <property name="enableSubPackages" value="true" />
            <property name="trimStrings" value="true" />
        </javaModelGenerator>

        <!-- SQL映射文件生成器 -->
        <sqlMapGenerator targetPackage="com.vincent.mybatixtest"
                         targetProject="src/main/resources">
            <property name="enableSubPackages" value="true" />
        </sqlMapGenerator>

        <!-- Mapper接口生成器 -->
        <javaClientGenerator type="XMLMAPPER"
                             targetPackage="com.vincent.mybatixtest.mapper" targetProject="src/main/java">
            <property name="enableSubPackages" value="true" />
        </javaClientGenerator>

        <!-- 表生成器 -->
        <table tableName="view1"></table>
    </context>
</generatorConfiguration>
